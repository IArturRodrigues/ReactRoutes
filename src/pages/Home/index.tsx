import { useState } from 'react';

interface HomeProps {
   children?: React.ReactNode;
}

export function Home({ }: HomeProps): JSX.Element {
   return (
      <div>

      </div>
   );
}

export default Home;